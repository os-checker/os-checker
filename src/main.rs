#[macro_use]
extern crate eyre;

use eyre::Result;

/// cli argument parsing
mod cli;
/// figure out the codebase layout
mod layout;
/// parse yaml file for repo configuration
mod repo;

fn main() {}
